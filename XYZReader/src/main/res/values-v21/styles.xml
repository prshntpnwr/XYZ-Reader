<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme.Main" parent="@style/AppTheme">
        <item name="android:windowContentTransitions">true</item>

    </style>

    <style name="AppTheme.Detail">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowContentTransitions">true</item>

        <!-- specify shared element transitions -->
        <item name="android:windowSharedElementEnterTransition">@transition/change_image_transform</item>
        <item name="android:windowSharedElementExitTransition">@transition/change_image_transform</item>
        <item name="android:windowEnterTransition">@transition/article_list_exit_transition</item>
        <item name="android:windowReturnTransition">@transition/article_list_return_transition</item>


    </style>
</resources>

